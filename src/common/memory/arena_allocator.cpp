#include "common/memory/arena_allocator.h"

// Реализация методов ArenaAllocator. 
// В данный момент вся логика inlined в заголовочном файле (header-only) для максимальной производительности
// (отсутствие накладных расходов на вызов функций на этапе выполнения).
// Данный файл оставлен как translation unit для будущих расширений (например, интеграции с NUMA-нодами).
